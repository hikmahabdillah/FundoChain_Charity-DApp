import React from "react";

const HeroSections = () => {
  return (
    <section id="hero" className="mt-24 min-h-screen w-full">
      HeroSections
    </section>
  );
};

export default HeroSections;
